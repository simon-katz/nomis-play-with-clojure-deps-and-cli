#!/usr/bin/env bash

# c.f. `cider-clojure-cli-aliases` in .dir-locals.clj.

# clj -M:project/dev:user/dev:user/cider-nrepl

echo 'We have gotten rid of the `:user/cider-nrepl` alias. Start a REPL from Emacs and then copy the startup command to here.'

exit 1
